# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: cowork-mcp-server
metadata:
  template: cowork-mcp-server
services:
  mcp-server:
    project: .
    language: python
    host: containerapp
    docker:
      path: Dockerfile
