# Central hub configuration (build-time, public). Copy to `.env.local` to override
# locally, or set these in your CI / Static Web App build.

# Absolute origin of the hub. Used as the metadata base for canonical / OpenGraph
# URLs. The hub always serves the domain root, so there is no basePath here.
NEXT_PUBLIC_HUB_URL=https://uzoniandev.com
